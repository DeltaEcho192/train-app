import 'package:flutter/material.dart';

class Model {
  String firstName;
  String lastName;
  String email;
  bool checkBox;
  String picName;
  Model(
      {this.firstName, this.lastName, this.email, this.checkBox, this.picName});
}
